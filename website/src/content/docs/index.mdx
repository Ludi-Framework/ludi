---
title: Ludi
description: An Express-style web framework for Lua, powered by Rust.
template: splash
hero:
  tagline: An Express-style web framework for Lua, powered by Rust.
  image:
    file: ../../assets/ludi_logo.png
    alt: Ludi logo
  actions:
    - text: Get Started
      link: /getting-started/installation/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/Ludi-Framework/ludi
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

```lua
local ludi = require("ludi")

local app = ludi.new()

app:get("/users/:id", function(req, res)
    res:json({ id = req.params.id })
end)

app:listen(3000, function()
    print("Listening on http://localhost:3000")
end)
```

```bash
luarocks install ludi
lua app.lua
```

<CardGrid stagger>
	<Card title="Express-style API" icon="rocket">
		Routing, middlewares and handlers are plain Lua, with the API you already
		know from Express and Fastify.
	</Card>
	<Card title="Rust core" icon="setting">
		A native module built on hyper + tokio does the HTTP heavy lifting: fast
		parsing, limits and socket I/O.
	</Card>
	<Card title="No extra runtime" icon="approve-check">
		No OpenResty, no nginx, no separate runtime — just `require("ludi")` and run
		your app with any Lua interpreter.
	</Card>
	<Card title="Middlewares" icon="puzzle">
		Global and per-route middleware chains with explicit `next()`,
		express-style.
	</Card>
</CardGrid>
